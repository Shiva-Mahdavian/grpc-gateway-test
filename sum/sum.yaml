type: google.api.Service
config_version: 3

http:
  rules:
    - selector: sum.SumComputer.ComputeSum
      post: "/test/sum"
      body: "*"
    - selcetor: randomSelector.Selector.Select
      post: "/test/random"
      body: "*"